# reference-pack

A minimal reference vertical pack for `libraos.simulator.load_pack(path=...)`.
Demonstrates: `pack.yaml` metadata, an archetype carrying extension fields, a
rubric case, and a `compliance/overlay.yaml` extension schema.
